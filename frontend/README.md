# Frontend

Vite + React + TypeScript. Dev server: `npm run dev` (usually started from repo root via `npm run dev`).

- **`src/`** — UI and API client (`api.ts`).
- **`VITE_API_BASE`** — Override backend URL (default `http://localhost:8000/api`); see `.env.example`.
